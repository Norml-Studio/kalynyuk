{
  "name": "anna-kalynyuk-theme",
  "version": "1.1.0",
  "private": true,
  "description": "Anna Kalynyuk — Norml Studio child theme. Build pipeline for the de-Divi migration.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "sass": "^1.80.0",
    "vite": "^7.0.0"
  },
  "browserslist": [
    "> 0.5%",
    "last 2 versions",
    "not dead"
  ]
}
